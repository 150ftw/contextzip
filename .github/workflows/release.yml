name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            cross: true
            update_name: linux-x86_64
          - name: linux-x86_64-musl
            target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            cross: true
          - name: macos-arm64
            target: aarch64-apple-darwin
            os: macos-latest
            cross: false
            update_name: darwin-aarch64
          - name: macos-x86_64
            target: x86_64-apple-darwin
            os: macos-latest
            cross: false
            update_name: darwin-x86_64
          - name: windows-x86_64
            target: x86_64-pc-windows-msvc
            os: windows-latest
            cross: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build with cross
        if: matrix.cross
        run: cross build --release --target ${{ matrix.target }}

      - name: Build native
        if: "!matrix.cross"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          cp target/${{ matrix.target }}/release/contextzip contextzip-${{ matrix.name }}

      - name: Rename binary (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cp target/${{ matrix.target }}/release/contextzip.exe contextzip-${{ matrix.name }}.exe

      # `contextzip update` (src/init.rs) expects contextzip-<os>-<arch>.tar.gz
      # containing a binary named `contextzip`.
      - name: Package update tarball
        if: matrix.update_name
        run: |
          tar -czf contextzip-${{ matrix.update_name }}.tar.gz -C target/${{ matrix.target }}/release contextzip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: contextzip-${{ matrix.name }}
          path: |
            contextzip-${{ matrix.name }}*
            contextzip-*.tar.gz

  checksums:
    name: Generate checksums
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Generate SHA256 checksums
        run: |
          mkdir -p release
          find artifacts -type f \( -name "contextzip-*" \) -exec cp {} release/ \;
          cd release
          sha256sum * > checksums-sha256.txt

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
